{
  "name": "taskflow-pwa",
  "version": "1.0.0",
  "private": true,
  "description": "TaskFlow — Todo & Reminder Widget (PWA + iOS via Capacitor)",
  "scripts": {
    "build": "bash scripts/copy-web.sh",
    "ios:setup": "npm run build && npx cap add ios && npx cap sync ios",
    "ios": "npm run build && npx cap sync ios && npx cap open ios",
    "ios:sync": "npm run build && npx cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/local-notifications": "^8.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
